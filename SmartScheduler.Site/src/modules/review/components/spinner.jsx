import React from 'react';

const Spinner = (props) => ({

    render: function() {
        // const {schedule} = this.props;

        return (
            <div className="spinner-screen">
            </div>);
    }
})

export default Spinner;